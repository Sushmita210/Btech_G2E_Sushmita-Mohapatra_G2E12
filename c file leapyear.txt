#include <stdio.h>
#include <math.h>
int main() {
int year;
printf("enter a number :\n");
scanf("%d", &year);

if((year%4==0) && (year%100 !=0) || (year%400==0)){
printf("year is leapyear");
}
else {
printf("not a leapyear");
}


return 0;
}